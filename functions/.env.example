# Copy to .env for local emulator testing only, or set SEED_SECRET in the
# Google Cloud Console → Cloud Run → seedProjects service → Variables.
# Used by POST /api/seed-projects with header x-seed-secret: <value>
SEED_SECRET=
